!!!
%html
  %head
    %title LetterOpenerDemo
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    #header
      %h1 The Best Site Ever
      .subtitle But we can't tell you why ... yet

      
    #content
      #flash_messages
        - flash.each do |key, value|
          .flash_message= value 
      = yield

    #footer
      == Copyright #{Time.now.year}
